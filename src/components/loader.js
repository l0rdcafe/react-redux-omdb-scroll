import React from "react";
import Spinner from "react-spinkit";

const Loading = () => <Spinner name="chasing-dots" color="#000" style={{ display: "block", margin: "auto", marginTop: "100px" }} />;

export default Loading;
